
  # Restaurant Website

  This is a React JSX restaurant website project built with Vite.

  ## Running the code

  Run `npm install` to install dependencies.

  Run `npm run dev` to start the development server.
  